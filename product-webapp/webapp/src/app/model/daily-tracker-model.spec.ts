import { DailyTrackerModel } from './daily-tracker-model';

describe('DailyTrackerModel', () => {
  it('should create an instance', () => {
    expect(new DailyTrackerModel()).toBeTruthy();
  });
});
